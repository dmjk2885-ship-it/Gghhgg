local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer

-- ล้าง UI เก่าหากมีการรันซ้ำ
if getgenv().SpeedHack_Gui then
	pcall(function() getgenv().SpeedHack_Gui:Destroy() end)
end
if getgenv().SpeedHack_Connection then
	pcall(function() getgenv().SpeedHack_Connection:Disconnect() end)
end

-- สร้าง ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "SpeedHackUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
getgenv().SpeedHack_Gui = ScreenGui

-- ข้อความแจ้งเตือนตอนเริ่มต้น
local NotifyLabel = Instance.new("TextLabel")
NotifyLabel.Size = UDim2.new(0, 380, 0, 75)
NotifyLabel.Position = UDim2.new(0.5, -190, 0.35, -37.5)
NotifyLabel.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
NotifyLabel.BorderSizePixel = 0
NotifyLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
NotifyLabel.Font = Enum.Font.GothamBold
NotifyLabel.TextSize = 18
NotifyLabel.Text = "กำลังโหลดระบบ..."
NotifyLabel.Parent = ScreenGui

local NotifyCorner = Instance.new("UICorner")
NotifyCorner.CornerRadius = UDim.new(0, 10)
NotifyCorner.Parent = NotifyLabel

local NotifyStroke = Instance.new("UIStroke")
NotifyStroke.Color = Color3.fromRGB(80, 80, 80)
NotifyStroke.Thickness = 1.5
NotifyStroke.Parent = NotifyLabel

-- Main Frame (หน้าต่างตั้งค่าความเร็ว)
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 220, 0, 150)
MainFrame.Position = UDim2.new(0.5, -110, 0.5, -75)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
MainFrame.BorderSizePixel = 0
MainFrame.Visible = false
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 10)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(60, 60, 60)
MainStroke.Thickness = 1.5
MainStroke.Parent = MainFrame

-- แสดงข้อความแจ้งเตือน + คัดลอกดิสคอร์ด
task.spawn(function()
	task.wait(1)
	NotifyLabel.Text = "คัดลอกลิ้งดิสสำเร็จเเล้ว!"
	NotifyLabel.TextColor3 = Color3.fromRGB(100, 255, 100)
	
	pcall(function()
		setclipboard("https://discord.gg/Ugssja4wqQ")
	end)
	
	task.wait(1.5)
	NotifyLabel:Destroy()
	MainFrame.Visible = true
end)

-- แถบ TopBar สำหรับลากย้ายหน้าต่าง
local TopBar = Instance.new("Frame")
TopBar.Size = UDim2.new(1, 0, 0, 30)
TopBar.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
TopBar.BorderSizePixel = 0
TopBar.Parent = MainFrame

local TopCorner = Instance.new("UICorner")
TopCorner.CornerRadius = UDim.new(0, 10)
TopCorner.Parent = TopBar

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, -10, 1, 0)
TitleLabel.Position = UDim2.new(0, 10, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "ระบบตั้งค่าความเร็ววิ่ง (WalkSpeed)"
TitleLabel.TextColor3 = Color3.fromRGB(220, 220, 220)
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextSize = 11
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = TopBar

-- ป้ายบอกทาง
local InputLabel = Instance.new("TextLabel")
InputLabel.Size = UDim2.new(1, -20, 0, 20)
InputLabel.Position = UDim2.new(0, 10, 0, 38)
InputLabel.BackgroundTransparency = 1
InputLabel.Text = "ใส่ตัวเลขความเร็ว (ปกติคือ 16):"
InputLabel.TextColor3 = Color3.fromRGB(180, 180, 180)
InputLabel.Font = Enum.Font.GothamBold
InputLabel.TextSize = 11
InputLabel.TextXAlignment = Enum.TextXAlignment.Left
InputLabel.Parent = MainFrame

-- ช่องใส่ตัวเลข (TextBox)
local SpeedBox = Instance.new("TextBox")
SpeedBox.Size = UDim2.new(1, -20, 0, 35)
SpeedBox.Position = UDim2.new(0, 10, 0, 60)
SpeedBox.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
SpeedBox.Text = "50"
SpeedBox.TextColor3 = Color3.fromRGB(255, 255, 255)
SpeedBox.Font = Enum.Font.GothamBold
SpeedBox.TextSize = 14
SpeedBox.ClearTextOnFocus = false
SpeedBox.Parent = MainFrame

local BoxCorner = Instance.new("UICorner")
BoxCorner.CornerRadius = UDim.new(0, 6)
BoxCorner.Parent = SpeedBox

-- ปุ่มสวิตช์ เปิด/ปิด
local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Size = UDim2.new(1, -20, 0, 35)
ToggleBtn.Position = UDim2.new(0, 10, 0, 102)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
ToggleBtn.Text = "สวิตช์ความเร็ว: ปิด"
ToggleBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.TextSize = 12
ToggleBtn.Parent = MainFrame

local BtnCorner = Instance.new("UICorner")
BtnCorner.CornerRadius = UDim.new(0, 8)
BtnCorner.Parent = ToggleBtn

-- ระบบลากหน้าต่าง UI
local dragging, dragStart, startPos
TopBar.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		dragging = true
		dragStart = input.Position
		startPos = MainFrame.Position
		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then
				dragging = false
			end
		end)
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
		local delta = input.Position - dragStart
		MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
	end
end)

-- การทำงานสวิตช์เปิด/ปิด
local isSpeedActive = false

ToggleBtn.MouseButton1Click:Connect(function()
	isSpeedActive = not isSpeedActive
	if isSpeedActive then
		ToggleBtn.Text = "สวิตช์ความเร็ว: เปิด"
		ToggleBtn.TextColor3 = Color3.fromRGB(100, 255, 100)
		ToggleBtn.BackgroundColor3 = Color3.fromRGB(25, 60, 25)
	else
		ToggleBtn.Text = "สวิตช์ความเร็ว: ปิด"
		ToggleBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
		ToggleBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
		
		-- รีเซ็ตความเร็วกลับเป็นค่าปกติ (16)
		local myChar = LocalPlayer.Character
		if myChar then
			local humanoid = myChar:FindFirstChildOfClass("Humanoid")
			if humanoid then
				humanoid.WalkSpeed = 16
			end
		end
	end
end)

-- ลูปบังคับความเร็ว (Lock WalkSpeed)
getgenv().SpeedHack_Connection = RunService.RenderStepped:Connect(function()
	if isSpeedActive then
		local myChar = LocalPlayer.Character
		if myChar then
			local humanoid = myChar:FindFirstChildOfClass("Humanoid")
			if humanoid then
				local speedValue = tonumber(SpeedBox.Text)
				if speedValue then
					humanoid.WalkSpeed = speedValue
				end
			end
		end
	end
end)
